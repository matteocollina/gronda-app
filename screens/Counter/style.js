import { StyleSheet } from "react-native";

export default style = StyleSheet.create({
    container: {
        display: 'flex',
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
      },
    number : {
        color: 'white',
            fontWeight: 'bold',
    },
    visits: {fontSize: 20, color: 'white',fontWeight: '700'}
})